package com.example.milos_achats.ui.theme

import androidx.compose.ui.graphics.Color

// ── Milos Brand Blue (extrait du logo) ───────────────────────────────────────
val MilosBlue          = Color(0xFF4E7FC4)   // couleur exacte du fond logo
val MilosBlueDark      = Color(0xFF2B5597)   // variante foncée
val MilosBlueDeep      = Color(0xFF0C2D5E)   // texte sur fond bleu clair
val MilosBlueContainer = Color(0xFFD4E5F7)   // fond de cartes / containers
val MilosBlueLight     = Color(0xFFEBF3FC)   // surface très légère

// ── Secondary : ambre café ────────────────────────────────────────────────────
val MilosCoffee          = Color(0xFFB8711A)  // ambre chaud, évoque le café
val MilosCoffeeDark      = Color(0xFF7A4500)
val MilosCoffeeContainer = Color(0xFFFCE8CC)  // fond carte secondaire
val MilosCoffeeOnLight   = Color(0xFF3E1F00)

// ── Sémantique : confirmé / alerte ───────────────────────────────────────────
val ConfirmedGreen    = Color(0xFF2E7D32)
val ConfirmedGreenBg  = Color(0xFF4CAF50)
val WarningAmber      = Color(0xFFD84315)
val WarningAmberBg    = Color(0xFFFF7043)

// ── Surfaces ─────────────────────────────────────────────────────────────────
val MilosSurface     = Color(0xFFF7F9FD)   // blanc légèrement bleuté
val MilosSurfaceDark = Color(0xFF151D2E)

// ── Dark mode ─────────────────────────────────────────────────────────────────
val MilosBlue80   = Color(0xFF9BBFE8)
val MilosCoffee80 = Color(0xFFFFBA6A)
